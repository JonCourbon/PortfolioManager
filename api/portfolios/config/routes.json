{
  "routes": [
    {
      "method": "GET",
      "path": "/portfolios",
      "handler": "portfolios.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/portfolios",
      "handler": "portfolios.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/portfolios",
      "handler": "portfolios.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
